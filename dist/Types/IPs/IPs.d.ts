export declare type IpsListResponseBody = {
    assignable_to_pools: boolean;
    items: string[];
    total_count: number;
};
export declare type IpData = {
    ip: string;
    dedicated: boolean;
    rdns: string;
};
